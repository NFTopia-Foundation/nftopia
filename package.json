{
  "name": "nftopia",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "workspaces": ["apps/frontend", "apps/backend", "contracts"],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pnpm run build -r", 
    "dev": "pnpm -r --parallel run dev",
    "lint": "pnpm run lint -r",
    "format": "pnpm run format -r"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.6.4"
}

