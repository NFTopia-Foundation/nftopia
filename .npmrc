# Enable isolated node_modules per package (for separate dependencies)
node-linker=isolated

# Store global cache in ~/.pnpm-store instead of node_modules/.pnpm
store-dir=~/.pnpm-store

# Enable strict peer dependency resolution (recommended for monorepos)
strict-peer-dependencies=true

# Auto-install missing peer dependencies
auto-install-peers=true
